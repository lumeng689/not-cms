var Q = require('q')
var request = require('request')

export default {
  login: (username, password, cb) => {
    return new Promise((resolve, reject) => {})
  },
  logout: (params) => {
    return new Promise((resolve, reject) => {})
  }
}
